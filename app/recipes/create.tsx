import { Page, StyledText } from "@/components/ui";

export default function CreateRecipeScreen() {
  return (
    <Page>
      <StyledText className="text-xl font-bold">Create Recipe</StyledText>
    </Page>
  );
}
